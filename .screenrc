# This file has screen config that should be done only once.
# Most config should go into .screenrc_reloadable, which is
# reloaded by a key binding.

# Start screen with a single layout
layout new

source ~/.screenrc_reloadable
